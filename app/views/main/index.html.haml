#header
  - if value_for_name "enable_featured_comic"
    Showing the featured comic!
    %br
    - comic = featured_comic
    - if comic.last_page != nil
      = render "pages/styled_page", :locals => {:page => comic.last_page}
      - else
      = "no page to display"
